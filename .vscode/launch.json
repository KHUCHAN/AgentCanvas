{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run AgentCanvas Extension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/extension/dist/**/*.js"
      ],
      "preLaunchTask": "agent-canvas: build"
    }
  ]
}
